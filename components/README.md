
### components フォルダ

ここにはコンポーネント・ライブラリを配置する。  
Arduino環境ビルド時のライブラリ探索先として、platformio.ini にて設定されている。  

例えば M5Unified + M5GFX をここに配置すれば ESP-IDF からも Arduino からも使用可能  
ここに配置するフォルダは実体でなくシンボリックリンクでも良い。  

なお ESP-IDF ビルドの際には、ここの構造を変えたら一度 clean し、クリーンビルドする必要がある。  

